
export default async function Gallery({ id }: { id: string }) {
	// const { data, error } = await getGallery(id)

	// console.log(data)
	// return (
	//     <><pre>{JSON.stringify(data)}</pre></>
	// )
}

async function getGallery(id: string) {
	// const { data, error } = await supabase.storage
	//     .from("project-images")
	//     .list(id + "/", {
	//         limit: 100,
	//         offset: 0,
	//     });

	// return { data, error }
}

